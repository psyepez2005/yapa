import 'package:flutter/material.dart';

class AppColors {
  // Colores principales (Aproximación a la marca)
  static const Color primaryBlue = Color(0xFF0033A0); // Azul corporativo
  static const Color accentPink = Color(0xFFE5007D);  // Rosa/Magenta vibrante
  
  // Colores de fondo y superficies
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;
  
  // Textos
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  
  // Estados de fidelización (Oro, Plata, Bronce, etc.)
  static const Color goldTier = Color(0xFFFFD700);
  static const Color success = Color(0xFF4CAF50);
}